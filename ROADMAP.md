# 小岛AI输入法 · 对标与路线图

对标对象：豆包输入法（AI 语音输入的体验标杆）。本项目差异化：**全本地转写、隐私零上传、开源可改**；AI 能力（润色）用用户自备的大模型 API Key，可插拔。

## 对标豆包输入法：能力差距清单

| 能力 | 豆包输入法 | 小岛AI输入法 | 状态 |
|---|---|---|---|
| 按住说话 / 松手上屏 | ✅ | ✅ | 已有 |
| 中英混说识别 | ✅（云端） | ✅（SenseVoice 本地，zh/yue/en/ja/ko） | 已有 |
| 自动标点 | ✅ | ✅（模型自带） | 已有 |
| 隐私（语音不上传） | ❌ 云端识别 | ✅ 全本地 | 优势 |
| 热键自定义 | ✅ | ✅ 设置菜单（左/右 Option、右 Command、F19） | **P1 本次** |
| AI 润色（去口水词/书面化） | ✅ 内置 | ✅ 可插拔 API（DeepSeek/Kimi/GLM/GPT/ollama…） | **P1 本次** |
| 热词/专有名词纠错 | ✅ 词库 | ✅ hotwords（注入润色提示词）+ replacements（离线替换表） | **P1 本次** |
| 设置界面 | ✅ 完整 GUI | ⚠️ 菜单栏 + settings.json | P1 简版 |
| 实时上屏（边说边出字） | ✅ | ❌（松手后一次性出字） | P3 |
| 多种润色风格（书面/翻译/纪要）+ **自定义风格** | ⚠️ 固定几种 | ✅ 内置 5 种 + settings 自定义任意风格 | **P2 已做，超越点** |
| **双击锁定录音**（长段口述免按住） | ❌ | ✅ 双击进入、再按结束 | **P2 已做，超越点** |
| 独立 App / 免终端授权 | ✅ | ✅ `scripts/make_app.sh` 一键打包 .app | **P2 已做** |
| 输入历史 | ✅ | ✅ 菜单最近 10 条点击复制，本地 jsonl | **P2 已做** |
| 声音反馈 | ✅ | ✅ 开始/结束/取消提示音（可关） | **P2 已做** |
| 跨设备词库同步 / 账号 | ✅ | 不做（保持本地纯粹） | 不做 |

## P1（本次已实现）

- **设置系统**：`settings.json`（gitignored）+ 默认值深合并；菜单栏「设置」入口；「重新加载配置」免重启生效。
- **热键可配**：左 Option（默认）/ 右 Option / 右 Command / F19，菜单单选即时生效。
- **AI 润色（可开关，默认关）**：转写后过一遍大模型，去口水词、修同音错字、规范标点；
  - provider `openai`（默认）：任意 OpenAI 兼容端点，DeepSeek / Kimi / GLM / OpenAI / ollama 本地模型均可；
  - provider `anthropic`：Anthropic API（需自备 key）；
  - **fail-open**：润色超时/报错一律回退原始转写，绝不阻断出字。
- **准确率辅助**：`hotwords` 热词表（注入润色提示词纠同音错字）+ `replacements` 离线替换表（不依赖 LLM、零延迟）。

## P2（本次已实现）

- ✅ 润色风格：内置「润色 / 书面化 / 轻度纠错 / 翻译成英文 / 会议纪要」+ settings 自定义风格，菜单一键切换。
- ✅ 双击锁定录音：双击热键进入持续录音（长段口述解放手指），再按一下结束；录音中按其他键取消。
- ✅ 输入历史：本地 `data/history.jsonl`，菜单最近 10 条点击复制。
- ✅ 提示音：录音开始/结束/取消系统音效，可配置关闭。
- ✅ 独立 .app：`scripts/make_app.sh` 生成 ad-hoc 签名的 App bundle，权限授给 App 本身，可进登录项自启。
- ⏳ 首次启动引导（权限检测 + 跳转设置页）——移入 P2.5。

## P2.5（第三轮已实现，借鉴 superwhisper / Wispr Flow）

- ✅ **场景感知润色**（superwhisper 王牌）：按前台 App（bundle id / 应用名）自动切换润色风格或关闭润色，`app_styles` 配置。
- ✅ **输入统计**（Wispr Flow 增长神器）：菜单栏累计段数/字数/预计节省时间。
- ✅ **录音计时**：录音中菜单栏显示已录秒数（锁定录音的「还在录」确认）。

## P3（第四轮已实现）

- ✅ **实时预览悬浮窗（伪流式）**：录音时 NSPanel 浮窗实时显示识别文本。真流式仅 Parakeet/Moonshine/Voxtral 支持（无中文），改用 SenseVoice 全量重转方案（每 ~0.7s 重转累积音频，36ms/3.5s 的速度使其可行，且自带回头修正），松手一次性上屏，避免"原地打字式"在任意 App 里删改的翻车风险。
- ✅ **语音指令改写**（Aqua Voice 同款）：选中文字 → 右 Option 说指令 → 哨兵值检测选区（Cmd+C）→ 选区+指令交给 LLM → Cmd+V 原地替换 → 恢复原剪贴板；无选区/模型失败一律 fail-open。

## P4（后续）

- 双语模式快捷切换（说中文出英文已可用「翻译成英文」风格，缺快捷切换）。
- 常驻润色通道：本地小模型（ollama qwen 系）做默认润色，无网络、无 key、~1s。
- 稳定签名身份（Apple 开发者证书），消除重打包后需重新授权的问题。

## 不做

- 云端账号体系、跨设备同步、词库云备份——与"全本地、隐私零上传"的定位冲突。
