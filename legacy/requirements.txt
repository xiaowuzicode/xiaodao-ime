# 跨平台核心
pynput>=1.8.0
sounddevice>=0.5.0
numpy>=2.0.0
transcribe-cpp>=0.1.3
huggingface_hub>=0.20.0

# macOS：菜单栏 + AppKit/Quartz 后端
rumps>=0.4.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=12.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=12.0; sys_platform == "darwin"

# Windows：托盘 + 图标绘制（tkinter/winsound/ctypes 为标准库）
pystray>=0.19.0; sys_platform == "win32"
pillow>=10.0.0; sys_platform == "win32"
